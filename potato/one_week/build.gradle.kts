plugins {
    id("com.android.application") version "8.8.0" // `agp` 버전
    id("org.jetbrains.kotlin.android") version "2.0.0" // `kotlin` 버전
    id("org.jetbrains.kotlin.plugin.compose") version "2.0.0"
}
